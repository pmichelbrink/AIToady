<Window x:Class="AIToady.Harvester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AIToady.Harvester"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="AI Toady Harvester" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <TextBox Width="300" Height="25" VerticalContentAlignment="Center" Text="{Binding Url}" KeyDown="UrlTextBox_KeyDown"/>
            <Button Content="Go" Width="50" Height="25" Margin="5,0,0,0" Command="{Binding GoCommand}"/>
            <Button Content="Next" Width="50" Height="25" Margin="5,0,0,0" Command="{Binding NextCommand}"/>
            <Button Content="Load Threads" Width="80" Height="25" Margin="5,0,0,0" Command="{Binding LoadThreadsCommand}"/>
            <Button Content="Start Harvesting" Width="100" Height="25" Margin="5,0,0,0" Command="{Binding StartHarvestingCommand}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <Label Content="Next Element:" Width="80" VerticalAlignment="Center"/>
            <TextBox x:Name="NextElementTextBox" Width="150" Height="25" VerticalContentAlignment="Center" Text="{Binding NextElement}" GotFocus="NextElementTextBox_GotFocus" LostFocus="NextElementTextBox_LostFocus"/>
            <Label Content="Thread Element:" Width="100" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <TextBox x:Name="ThreadElementTextBox" Width="150" Height="25" VerticalContentAlignment="Center" Text="{Binding ThreadElement}" GotFocus="ThreadElementTextBox_GotFocus" LostFocus="ThreadElementTextBox_LostFocus"/>
            <Label Content="Page Load Delay (seconds):" Width="150" VerticalAlignment="Center" Margin="10,0,0,0"/>
            <TextBox Width="30" Height="25" VerticalContentAlignment="Center" Text="{Binding PageLoadDelay}"/>
        </StackPanel>
        
        <wv2:WebView2 x:Name="WebView" Grid.Row="2" Margin="5"/>
    </Grid>
</Window>
